<Window x:Class="NodicaEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NodicaEditor"
        xmlns:nodica="clr-namespace:Nodica;assembly=Nodica"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280"
        Background="#1A1A1A">

    <Window.Resources>
        <!-- Define uniform styles for controls -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="75"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#D4D4D4"/>
        </Style>

        <!-- HierarchicalDataTemplate for TreeView, named as NodeTemplate -->
        <HierarchicalDataTemplate x:Key="NodeTemplate" DataType="{x:Type nodica:Node}" ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="EditStates">
                        <VisualState x:Name="Normal"/>
                        <VisualState x:Name="Edit">
                            <Storyboard>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="textBlock"
                                                               Storyboard.TargetProperty="Visibility">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="Collapsed"/>
                                </ObjectAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="textBox"
                                                               Storyboard.TargetProperty="Visibility">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="Visible"/>
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
                <!-- Non-editable TextBlock -->
                <TextBlock x:Name="textBlock" Text="{Binding Name}" MouseLeftButtonDown="TextBlock_MouseLeftButtonDown" />
                <!-- Editable TextBox -->
                <TextBox x:Name="textBox" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Visibility="Collapsed" KeyDown="TextBox_KeyDown" LostFocus="TextBox_LostFocus"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <ContentPresenter Content="{Binding}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="#2A2A2A" Foreground="#D4D4D4">
            <MenuItem Header="_File">
                <MenuItem Header="_Open INI File..." Click="OpenIniFile_Click"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" MinWidth="250" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="400" MinWidth="250" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="5" />
                <RowDefinition Height="200" MinHeight="100" />
            </Grid.RowDefinitions>

            <!-- Scene Hierarchy (Left Panel) -->
            <TreeView Name="SceneHierarchyTreeView" Grid.Column="0" Grid.RowSpan="3" Background="#1A1A1A" Foreground="#D4D4D4"
                      SelectedItemChanged="SceneHierarchyTreeView_SelectedItemChanged"
                      ItemTemplate="{StaticResource NodeTemplate}">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="Foreground" Value="#D4D4D4"/>
                        <Setter Property="Background" Value="#2A2A2A"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True">
                                <Setter Property="Background" Value="#322222"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>

            <!-- Left Splitter -->
            <GridSplitter Grid.Column="1" Grid.RowSpan="3" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext" Background="#2A2A2A" />

            <!-- Main Content Area -->
            <Border Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Background="#1A1A1A" />

            <!-- Bottom Panel -->
            <Border Grid.Column="2" Grid.Row="2" Background="#1A1A1A" BorderBrush="#3C3C3C" BorderThickness="1">
                <TextBlock Text="Bottom Panel" Foreground="#D4D4D4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>

            <!-- Right Splitter -->
            <GridSplitter Grid.Column="3" Grid.RowSpan="3" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext" Background="#2A2A2A" />

            <!-- Inspector Panel (Right Panel) -->
            <ScrollViewer Grid.Column="4" Grid.RowSpan="3" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <StackPanel x:Name="InspectorPanel" Background="#1A1A1A" Margin="10">
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>